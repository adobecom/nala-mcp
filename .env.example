# NALA MCP Environment Configuration
# Copy this file to .env and update with your local paths

# ============================================
# Project Configuration (Choose One or Both)
# ============================================
# You can configure MAS only, Milo only, or both projects.
# At least ONE project path must be set.

# For MAS-only teams: Set only MAS_PROJECT_PATH (comment out MILO_PROJECT_PATH)
# For Milo-only teams: Set only MILO_PROJECT_PATH (comment out MAS_PROJECT_PATH)
# For multi-project teams: Set both paths

# MAS Project Path (OPTIONAL - comment out if not using MAS)
# Absolute path to your MAS (Merch at Scale) project
# Example: /Users/yourname/projects/mas
MAS_PROJECT_PATH=/path/to/your/mas/project

# Milo Project Path (OPTIONAL - comment out if not using Milo)
# Absolute path to your Milo project
# Example: /Users/yourname/projects/milo
MILO_PROJECT_PATH=/path/to/your/milo/project

# ============================================
# Default Project Settings
# ============================================
# Which project to use by default (mas or milo)
# Only relevant if you have BOTH projects configured above
DEFAULT_PROJECT=mas

# ============================================
# Optional Overrides
# ============================================
# Override the test output path (default: nala)
# TEST_OUTPUT_PATH=nala

# ============================================
# Test Execution Settings (Optional)
# ============================================
# Adobe IMS credentials for test execution (ask team for test account)
# IMS_EMAIL=your-test-email@adobetest.com
# IMS_PASS=your-test-password

# ============================================
# Usage Instructions
# ============================================
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Update the paths above with your local project paths
#
# 3. Start the MCP server:
#    npm start
#
# 4. For Claude Desktop, you can also set env vars in your MCP config:
#    {
#      "mcpServers": {
#        "nala-mcp": {
#          "command": "node",
#          "args": ["./src/index.js"],
#          "env": {
#            "MAS_PROJECT_PATH": "/your/local/path/mas",
#            "MILO_PROJECT_PATH": "/your/local/path/milo"
#          }
#        }
#      }
#    }
